<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="innopolis.innopass">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".views.activities.LoginActivity"
            android:launchMode="standard">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".views.activities.RegistrationActivity"
            android:launchMode="standard">

        </activity>
        <activity android:name=".views.activities.AdminProfileActivity"
            android:launchMode="singleTask">

        </activity>
        <activity android:name=".views.activities.UserProfileActivity"
            android:launchMode="singleTask">

        </activity>
        <activity android:name=".views.activities.SettingsActivity"
            android:launchMode="standard">

        </activity>
    </application>

</manifest>